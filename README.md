# EECS581_Project_3

Automated Scheduler

## Requirements

* Python 3.x.x
* Django
* Ics
* Pytz

## Getting Started

Create an env with Python 3.x.x
Install the requirements in the 'requirements.txt'
    run this cmd inside the env: "pip install -r requirements.txt"

To start the web app:

    From the base directory, run python3 auto_scheduler/manage.py runserver.

## Git Workflow

For information on how to rebase your branch on top of new changes in main, see the [Git Rebase Guide](docs/GIT_REBASE_GUIDE.md).
